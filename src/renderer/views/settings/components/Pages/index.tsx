import React, { Fragment } from 'react';
import { StyledTitle } from '../App/styles';

export const Pages = () => {
    return (
        <Fragment>
            <StyledTitle>起動時、ホーム、新しいタブ</StyledTitle>
        </Fragment>
    );
};
